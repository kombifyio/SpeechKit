IMPORTANT: This Windows release is intentionally unsigned.

SpeechKit currently has no available no-cost public code-signing path. Windows SmartScreen and installer trust warnings are expected. This is a manual-only Windows release.

Verify downloads through this release's SHA256SUMS.txt file. Auto-update requires signed Windows installers and will not treat this installer as a verified in-app update payload.
