org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# KSP2 is the only backend as of KSP 2.3.x -- KSP1 was removed, so the former
# ksp.useKSP2=false pin is gone. It existed because Room 2.6.1 and Moshi 1.15.1
# crashed KSP2 with "unexpected jvm signature V"; Room is now 2.8.4.
