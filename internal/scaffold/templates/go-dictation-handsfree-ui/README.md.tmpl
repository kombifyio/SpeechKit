# {{ .APP_NAME }}

Go-only SpeechKit Dictation scaffold for Hands-Free activation with UI commit.

This is not a no-UI assistant. Wake activation dispatches a `mode.start` command and the host UI chooses the text target and explicit commit surface.

Run:

```sh
go mod tidy
go run .
```
